# 项目更新日志

本文档记录了 `MusicPlayer` 项目自重构以来的所有重要变更。

## 版本 2.0 (核心重构与功能完善)

_发布日期: 2024-07-28_

### ✨ 新功能

- **三级歌词系统**:
  1.  **优先**从音乐文件内嵌的 ID3 标签中读取歌词。
  2.  若无，则自动匹配并加载与歌曲文件同名的 `.lrc` 歌词文件。
  3.  若仍无，则作为最后手段，尝试从 `lyrics.ovh` 公共 API 在线获取歌词。
- **单曲循环**: 当播放列表中只有一首歌曲时，播放结束后会自动重新循环播放。
- **艺术家与歌名显示**: UI 现在可以从文件的元数据中解析并显示艺术家和歌曲标题，如果元数据缺失，则回退显示文件名。

### 🐛 Bug 修复

- **循环播放逻辑**: 修复了因事件冲突导致单曲循环功能失效的 Bug。引入了状态锁以确保重置逻辑的原子性。
- **拖拽后状态不同步**: 彻底解决了拖拽进度条后，播放状态（如专辑封面旋转）与实际不符的问题。关键在于重构了 `MusicPlayer.js` 对 `onended` 事件的处理。
- **暂停后进度条归零**: 修复了暂停时，因逻辑顺序错误导致进度条清零的 Bug。
- **拖拽时音乐暂停**: 根据用户要求，重构了拖拽逻辑，实现了在拖拽进度条时音乐不中断并平滑过渡。
- **浏览器挂起后崩溃**: 增强了 `MusicPlayer.js` 的韧性，使其在 `AudioContext` 被浏览器关闭后能自动重新初始化，避免崩溃。
- **布局问题**: 修复了在显示两行（艺术家和歌曲名）文本后，进度条被挤出容器的 CSS 布局问题。

### 🚀 性能与体验优化

- **拖拽体验**:
  - 通过将事件监听器绑定到全局 `document`，实现了在屏幕任何位置都能流畅拖拽。
  - 在拖拽开始时缓存了元素尺寸，避免了在拖拽过程中频繁引发页面重排，提高了性能。
  - 使用 `requestAnimationFrame` 来更新进度条 UI，使动画与浏览器渲染同步，效果更流畅。
- **动态过渡效果**: 通过动态添加/移除 CSS 类，实现了点击进度条时有平滑过渡动画，而按住拖拽时无动画的即时响应效果。

### 🏗️ 代码质量与重构

- **核心音频逻辑 (`MusicPlayer.js`)**: 对该文件进行了彻底的重构，引入了清晰的状态管理 (`isPlaying`, `isPaused`)，精确的时间计算和健壮的资源管理，从根本上解决了大量 Bug。
- **抽象 `useDrag` 组合式函数**: 将所有复杂的拖拽逻辑从 `index.vue` 中抽离，封装到可复用的 `useDrag.js` 文件中，极大地简化了主组件的代码。
- **组件封装 (`Control.vue`)**: 将控制按钮从 `div` 升级为语义化的 `<button>` 元素，增强了可访问性 (Accessibility)。同时将点击事件处理封装在组件内部，使其更加独立。
- **代码注释**: 将项目核心文件 (`index.vue`, `MusicPlayer.js`, `useDrag.js`, `utils.js`, `Control.vue`) 的所有注释统一翻译为中文，提高了代码的可读性和可维护性。
